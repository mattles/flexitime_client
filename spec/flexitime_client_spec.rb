require 'spec_helper'

describe FlexitimeClient do
  it 'has a version number' do
    expect(FlexitimeClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
